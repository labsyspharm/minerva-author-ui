import React from 'react';
import { CustomPicker } from 'react-color';

class HuePicker extends React.Component {
  render() {
    return <div>MyColorPicker</div>;
  }
}

export default CustomPicker(HuePicker);
